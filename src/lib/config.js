// config.js
// import { PUBLIC_SSE_URL } from '$env/static/public';

// const isDevelopment = process.env.NODE_ENV === 'development';

// export const serverAddress = isDevelopment ? 'http://localhost:3000' : PUBLIC_SSE_URL;
